# Lance 内容工作台：使用报告与修改意见

日期：2026-09-14。依据：你的两份沟通记录、最新强制交付要求、仓库代码以及本地测试。文档里“已实现”的历史描述不作为功能验收证据。

版本说明：下文记录首次本地交付时的状态；后续按你的要求同步代码到GitHub，不改变真实AI尚待验收、后端仍需本机运行的结论。

## 结论

你需要的是个人内容生产系统：从想法形成可汇报方案，通过后再深化到可执行的拍摄工程。旧版功能入口很多，但生成、保存、验收、导出没有可靠衔接。按照你允许直接修改的要求，已重做主工作台，保留旧版和原始数据。

**当前交付是可运行的本地开发版本，不是已完成真实 AI 验收的最终系统。** 本机未配置新的 API Key 和三个实际模型 ID；没有消耗你的 AI 额度，没有生成真实的创意图或视频。没有推送或部署到 GitHub。

## 旧版为什么容易失败

| 问题 | 代码证据 | 对使用的影响 | 本次处理 |
| --- | --- | --- | --- |
| “导出PPT”并非真正PPT | `legacy.html` 的 `exportPlanReport` 生成 `.md`；另一汇报导出生成 HTML | 你找不到预期的 `.pptx`，不是简单地没找到保存目录 | 新增真正PPT生成，图片视频内嵌，成果可重复下载 |
| 一键生成弹窗位置错误 | `fullPlanModal` 位于导出报告的模板字符串内 | 主页面按钮找不到应操作的弹窗 | 重建主页面、任务和表单，不再依赖该弹窗 |
| 部分AI错误被吞掉 | 一键分镜流程 `catch` 只打印日志，之后继续显示“生成完成” | 看起来成功，实际上仍是空分镜 | 严格检查返回结构，失败显示缺项且不覆盖原稿 |
| 验收不匹配你的标准 | 旧生成流程没有完整检查3段各8秒、6条各自齐备、25镜与成片的一致性 | 提示词、空格子、时长错误容易混入成果 | 统一验收规则，实际核验文件与视频时长 |
| 画面风格容易模板化 | 旧分镜提示统一使用“造势型”“悬念到号召”等结构 | 不适合所有走心片、人物片和日常内容 | 以项目目标、已确认方向、真实场地与所选造型为上下文 |

这些是可确认的代码缺陷；不能据此断言你过去每次失败都由同一原因造成。账号权限、模型兼容、配额与上游报错仍需真实调用定位。

## 强制标准在新版的落点

### 快速单条

保留创意大纲、寓意、描述、台词、影像氛围、摄影调性及完整故事脚本。要求3–5张有效图片，开场／中间／结尾三个有效AI视频，各8秒，允许编码误差0.15秒。缺项时仅能预览，不允许导出完整汇报或确认方向。文字描述不计作图片／视频。

### 整体专场

整体大纲、描述、寓意、场景搭建、美术制景、影像氛围、摄影调性，加6条不同的故事脚本。每条独立满足快速标准，即18–30张图、18段视频，共144秒。整体字段目前是可编辑策划文字；如要专场级独立美术图册与场地示意图，也应在下一轮真实案例中纳入专场PPT，不应只停留在资料库。

### 深入优化

已实现汇报确认快照、12类执行方案、25镜拆解、逐镜AI图片／视频、单张修改时引用当前图、版本切换、锁定与保留旧版。25镜视频按顺序合成，保留已有音轨并可混入上传配乐。镜头或配乐改变会使旧成片标记为需更新，但不删除旧素材。深化PPT包含执行方案、25镜图和整条参考片。

完整成片在此指**AI分镜视频的顺序预演片**，不等于自动完成专业剪辑、准确口型、表演、配音与连续性审片。上述创意质量还需要实际模型和人工审片验收。

### 场景与服装

场景专题可存尺寸图、照片、视频、现有参考，关联专场，记录已确认尺寸与搭建限制，再由看图模型分析和图片模型生成／修改。未知尺寸不得冒充准确数据，效果图不是施工图。

服装专题可按分类保存历史视频、提取12个候选帧并保留时间点和来源视频，由看图模型识别造型、给出推荐，再手选项目依据。目前是均匀采样，不是精确换装检测，也还不是服装抠图或虚拟试衣。

TMD与My数据分开；审美库共享。个人偏好、项目选中造型和确认稿参与生成。尚未实现自动训练偏好模型、审美素材自动检索或无感云同步。

## 本次实测结果

测试使用人工合成的图形和视频，模拟媒体输入；不冒充真实AI素材，也不作为你正式项目的成果。

- 8项 JavaScript 测试通过：快速缺项、异常时长、6条专场、版本锁定、错误JSON、25镜与成片一致性、真实PPT内嵌结构、不完整禁止导出。
- 5项后端测试通过：来源及文件访问限制、视频请求参数、上传转MP4与范围读取、12帧提取与25镜实际合成、缺模型明确失败。25个2秒测试镜头合成约50秒，并保留声音。
- 隔离浏览器实操通过：创建脚本、编辑保存、刷新恢复、缺项阻止导出、模型未配置时不覆盖原稿、上传3图与3段8秒视频、下载PPT、刷新后再次下载、深化预览、双身份隔离、专场和场地表单、移动端无横向溢出。
- 单条测试PPT共14页；专场结构测试含18段内嵌视频；深化结构测试共65页。用LibreOffice打开并转PDF检查中文和版面。原生视频播放仍需在你实际使用的WPS／PowerPoint版本再验收。
- 代码语法检查与补丁空白检查通过。没有运行真实AI生成，因此生成速度、费用、视觉质量、视频连续性和模型参数兼容尚无实测结论。

## 你问的“快速生成PPT最后在哪里”

旧版相关按钮导出的是Markdown或HTML，并没有对应的真正PowerPoint文件。也不能仅凭仓库确认你过去是否曾成功下载过其他文件。

新版生成完成后有两个入口：浏览器默认下载目录中的 `.pptx`；同一个浏览器、同一个地址下的“成果与备份 → 重新下载”。媒体保存在项目的 `.lance-data/media/`。清理浏览器数据会清除文字和PPT缓存，请另外保存下载文件、定期导出ZIP。ZIP当前备份文字与引用素材，PPT需另外下载。

## 接下来怎么改，才真正接近你的工作

1. **先做一条真实验收片。** 用你真实的主题、产品依据、场地图和服装视频；配置已开通的实际模型，验证3–5图和三个8秒视频，再拿PPT在WPS播放。记录耗时、费用、失败原因，先不扩大到整场。
2. **再验收一整场六条。** 六条要承担不同内容任务，不只是改标题；统一人物和项目美术，允许各片情绪与摄影方式不同。加入明确的“选用／退回／修改理由”。
3. **深化一条到25镜。** 逐镜检查人物服装、空间方向、镜头衔接、台词时长；加入侧边并排对比、批注和镜头间继承。当前已有版本切换，尚不是专业审片平台。
4. **增强可落地性。** 场地增加带单位的尺寸标注、机位与灯位图、道具清单、预算区间及来源、安装与拍摄工时；AI不负责确认未知尺寸和安全施工条件。
5. **完善资料检索和长期存储。** 服装视频增加换装检测、重复造型合并和来源跳转；审美库支持项目选用与检索。真实流程通过后再增加登录、云数据库、对象存储和后台任务队列，避免把本机版误当多设备成品。

## 下一次真实测试需要什么

在本机连接设置或 `.env` 配置**新密钥**及文本、图片、视频模型ID，不要继续在聊天截图中公开密钥。再选一条实际脚本，提供一份场地图和一段有代表性的历史服装视频。

之前截图中暴露的API Key、AK/SK及Supabase高权限密钥，即使是测试用也应立即轮换。没有替你撤销账号凭据或清理Git历史。
