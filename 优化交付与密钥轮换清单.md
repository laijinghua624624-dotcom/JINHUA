# 分阶段内容工作台 · 本轮交付

## 使用变化

- 新建工作专场时填写计划片数，之后可调整为 1–100 条。默认值 6 只是起始值，不再是硬性规则。不能直接把片数降到已关联脚本以下，避免丢稿。
- 方向阶段：整体主张、推荐依据、与计划数量一致的故事分工；可导出“老板决策版”。不强制生图、生视频，不把未完成的素材标为完成。
- 完整交付：每条保留完整创意与脚本、A/B/C 封面、3–5 张视觉参考、开场/中间/结尾各 8 秒 AI 视频。批量生成前另行确认费用。
- 文字方向可以保存快照并开始执行方案、25 镜拆解。最终执行版仍检查完整确认稿、25 张 AI 分镜图与成片，不以提示词替代媒体。
- AI 专场规划只补充缺少的故事，保留已有脚本；数量或结构无效时不部分覆盖项目。
- 豆包转写在录音详情中主动发起；转写保存为候选，人工核对后追加文字，原录音与文字历史保留。
- 审美库新增“AI语义检索”，范围是当前空间、文件夹、分类与收藏筛选。最多 100 条一批；描述和封面图在确认后发往模型，相同内容与模型复用本机缓存。未下载的视频/链接不会假装已经理解。

## 模型配置与真实状态

模型分工和后端接口已接入，但当前本机服务没有有效凭据和已验证模型 ID，**尚未完成六个指定模型的真实调用验证**。界面展示的产品名称不是 API Model ID，不能直接等同。

| 用途 | 目标模型 | 服务端配置 |
|---|---|---|
| 整体创意、脚本、深化、视频反推 | Doubao-Seed-2.1-pro | `ARK_DIRECTOR_MODEL` |
| 碎片提炼与组合 | Doubao-Seed-2.1-turbo | `ARK_REFINE_MODEL` |
| 参考图、封面、单图打磨 | Seedream-5.0-lite | `ARK_IMAGE_MODEL` |
| 8秒预演、分镜视频 | Seedance-2.0-fast | `ARK_VIDEO_MODEL` |
| 录音转写 | 豆包录音文件识别2.0 | `SPEECH_API_KEY` 或旧版 App ID/Token；版本须在账户中确认 |
| 审美检索 | Doubao-embedding-vision | `ARK_EMBEDDING_MODEL` |

全部只配置在 `.env`，不放进聊天、网页或 Git。Ark 与语音凭据不同；不需要的 AK/SK 和 Supabase 服务端密钥不要为新版额外配置。无法调用时明确报错，不暗中换模型。

语音适配的是官方 [大模型录音文件极速识别接口](https://www.volcengine.com/docs/6561/1631584?lang=zh)，资源为 `volc.bigasr.auc_turbo`。是否支持账户内的 2.0 版本仍须核实，不宣称已经用上 2.0。浏览器 WebM 缺少时长时先做音频容器转换，原文件保留。向量接口结构核对自 [火山引擎官方 SDK](https://github.com/volcengine/volcengine-python-sdk/blob/master/volcenginesdkarkruntime/resources/multimodal_embeddings.py)。

## 安全处理与尚未完成项

已做：新版移除浏览器密钥输入，服务端不接受客户端模型/密钥覆盖；错误信息隐藏已配置凭据；旧版仅保留本地查看整理导出，停用云同步与直接联网调用；访问页面时清理指定旧设置对象内的凭据，不删除作品、脚本、灵感和参考资料。新增当前文件的凭据扫描及 CI。

**未做完：云端密钥尚未确认撤销/换新，Git 历史尚未重写。** 删除源码或浏览器设置不会让云端密钥失效。扫描发现历史 `api-guide.html` 和 `jimeng_proxy.py` 曾保存访问密钥，后者含配套 Secret；扫描只报告位置和指纹，不输出密钥值。截图里曝光的其他凭据也必须处理。

需要账户所有者完成的操作：

1. 火山方舟：在 API Key 管理中替换截图里曝光的旧 Key，验证新 Key 后撤销旧 Key。新 Key 只交给本机环境变量。
2. 火山引擎访问控制：检查 `Lance` 子用户及主账户的访问密钥。替换已曝光的 AK/SK；若旧即梦代理已不再使用，则撤销旧凭据而不是再创建不必要的高权限密钥。
3. [Supabase 原项目 API Keys](https://supabase.com/dashboard/project/wjvxhhqujkzoxglzqokr/settings/api-keys)：已定位 `Secret keys` 下的 `default`，右侧菜单有 `Delete API key`。先确认是否还有其他系统使用，再由你完成替换/撤销。不要点击删除项目；不要把 secret key 当成前端公开 key。若旧 JWT `service_role` 也曾曝光，还须按 Supabase 文档处理旧密钥；不要无差别更换 JWT 签名导致其他系统会话失效。
4. 完成新凭据配置后重启本机服务，做少量真实调用与旧 Key 失效核验。配置读取成功不等于生成成功。
5. 历史清理需单独确认重写范围、备份和强推；本轮没有擅自重写远端历史，也未改动数据库或删除项目。

## 验收范围

7 条方向规划、增量补齐、非法数量/重复关联防护、无媒体方向 PPT 导出、完整交付阻断、前端无密钥输入、空间隔离和移动布局已测试。模型返回使用模拟数据，不代表真实云模型生成质量或权限已经验证。

本机测试：`npm test`；`node tests/workflow-browser.cjs`；其他浏览器回归见 `tests/`。
当前文件扫描：`python3 scripts/audit_secrets.py`；历史审计：追加 `--history`，历史曝光尚未处理时应返回非零。
