# A domain you control, already pointing at the deployment server.
LANCE_DOMAIN=studio.example.com
# Single-quote the real bcrypt hash to preserve its dollar signs.
# Generate interactively with: docker run --rm -it caddy:2.11-alpine caddy hash-password
LANCE_PASSWORD_HASH=''
