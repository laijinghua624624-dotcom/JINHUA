name: Credential audit
on: [push, pull_request, workflow_dispatch]
permissions:
  contents: read
jobs:
  current-tree:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Detect known secret formats without printing values
        run: python3 scripts/audit_secrets.py
